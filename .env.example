FLASK_SECRET_KEY=change-me-in-production
ANTHROPIC_API_KEY=sk-ant-...
DATABASE_URL=sqlite:///instance/ledgeros.db
FLASK_ENV=development

# SMTP — leave blank to log emails to console only (dev mode)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=no-reply@marsoud.app
SMTP_FROM_NAME=Marsoud
SMTP_USE_TLS=true
